// This doesn't work without .ts, hopefully will be fixed using new CLI of prettier
// eslint-disable-next-line no-restricted-exports, import-x/extensions
export { config as default } from "@ra/format/config.ts";
